"""ClaimDesk - the application under test.

This package is a FIXTURE. It exists so the automation framework has something
realistic to test: authentication, roles, a state machine, monetary boundaries and
an audit trail.

It is deliberately small, and it is deliberately unaware of the tests. The
framework never imports it - see docs/adr/0002-black-box-boundary.md.
"""

__version__ = "1.0.0"
